import React from "react";
import NavLinks from "../NavLinks";

const Navbar = () => {
  return (
    <nav id="navbar">
      <NavLinks className="navbar" />
    </nav>
  );
};

export default Navbar;
